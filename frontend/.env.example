# Recommended for Vercel + rewrite proxy mode
# Backend Railway URL, no trailing slash
API_BASE_URL=https://your-railway-backend-domain

# Optional direct-browser mode (not recommended)
# If set, the frontend bypasses /backend rewrite and calls this URL directly.
# NEXT_PUBLIC_API_BASE_URL=https://your-railway-backend-domain
